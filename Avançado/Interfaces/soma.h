/*
* . Um arquivo .h é um arquivo de cabeçalho, nele colocamos apenas definições (interfaces) que serão implementadas em outro arquivo .c;
*/

/*
* . Cabeçalho de função: é a definição de uma função sem escopo. Nesta definição apenas dizemos qual o tipo de retorno, o nome da
* função e os parâmetros que ela recebe;
*/

int soma(int n1, int n2);
