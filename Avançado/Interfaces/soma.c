/*
* . Este arquivo .c apenas implementa a função definida no arquivo soma.h;
*/
#include "soma.h" // Importamos colocando o caminho do arquivo soma.h entre aspas ("").
                  // Se o arquivo .h estiver no mesmo diretório, basta colocarmos o nome para encontrarmos.

int soma(int n1, int n2) {
    return (n1 + n2);
}
